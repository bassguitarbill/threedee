class InputMap {
  // Mapping of strings to inputs
  // Should be able to query this class to determine if an input is being pressed
  // Boolean inputs (keyboard keys) as well as numeric inputs (joystick axes)
}

export default InputMap;